# Build output
.gradle/
build/
app/build/
captures/
*.apk
*.ap_
*.dex
*.class

# Local machine config (SDK path is per-machine; CI sets ANDROID_HOME)
local.properties

# Decoded keystore — regenerated from keystore/debug.keystore.base64 at build time.
# The base64 source IS committed (stable signing); the raw keystore is not.
/keystore/debug.keystore

# IDE
.idea/
*.iml
.DS_Store

# Logs / crash dumps
*.log
hs_err_pid*
replay_pid*

# Kotlin
.kotlin/

# NOTE: gradle/wrapper/gradle-wrapper.jar is intentionally committed (CI needs it).
